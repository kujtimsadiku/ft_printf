#include "ft_printf.h"

int	ft_printoctal(t_data *ap, unsigned int u)
{
	static char digit[10];
	char *prefix;
	int base;

	base = 8;
	digit[] = "0123456789";
	if (ap->hash == TRUE)
		prefix = "0";
	while (u != 0)
	{
		
	}
}